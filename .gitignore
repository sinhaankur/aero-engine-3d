node_modules
dist
.DS_Store
*.local
.env
.env.*
.vite

# Blender working files (keep exported glTF in public/models, not source .blend)
*.blend1
*.blend2

# local env (e.g. VITE_FLIGHT_API pointing at a dev worker)
.env.local
.env
